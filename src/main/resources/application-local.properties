# Application name
spring.application.name=ibha

# Datasource
spring.datasource.url=jdbc:mysql://localhost:3306/ibh?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Inam@4a6
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Server port (optional, change if 8080 is busy)
#server.port=8080
# fall back to 8080 if PORT is not set (useful locally)
server.port=${PORT:8080}
